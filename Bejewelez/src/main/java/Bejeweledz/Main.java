package Bejeweledz;


import javafx.application.Application;
import javafx.scene.Parent;
import javafx.scene.Scene;
import javafx.scene.Group;
import javafx.scene.image.Image;
import javafx.scene.image.ImageView;
import javafx.scene.layout.Pane;
import javafx.scene.paint.Color;
import javafx.scene.shape.Circle;
import javafx.stage.Stage;
import javafx.stage.StageStyle;
import javax.swing.GroupLayout;

public class Main  {

   /* private static final int Leveys = 6;
    private static final int Korkeus = 6;
    private static final int SIZE = 100;

    public static void main(String[] args) {
      //  launch(args);
    }

    public void aloita(Stage primaryStage) throws Exception {
        Group root = new Group();
        
        ImageView taustakuva = new ImageView(new Image("Documents/Bejewelez.game/taustakuva.jpg"));
        root.getChildren().add(taustakuva);
        
        Scene scene = new Scene(root, 400, 400, Color.TRANSPARENT);
        primaryStage.initStyle(StageStyle.TRANSPARENT);
        
        
        primaryStage.setScene(scene);
        primaryStage.show();
    }

    private Parent createContent() {
        Pane root = new Pane();
        root.setPrefSize(Leveys * SIZE, Korkeus * SIZE);
        return root;
    } */

}
